[package]
name = "stm32g4-spa-model"
version = "0.1.0"
edition = "2024"

[dependencies]
phm = { package = "proto-hal-model", git = "https://github.com/adinack/proto-hal" }
peripherals = { package = "stm32-spa-peripherals", path = "../../peripherals" }
